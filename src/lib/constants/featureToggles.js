// Central feature toggle flags for easy runtime configuration.
// Flip these booleans and redeploy to enable/disable related UI features.

// When true, product showcase videos (embedded iframes) are suppressed everywhere
// and layouts behave as if the variant has no video available.
export const HIDE_PRODUCT_VIDEOS = true; // toggle as needed

// (Add future toggles below)
// export const SOME_OTHER_FLAG = false;
